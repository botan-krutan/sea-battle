using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ShipOne : ShipBase
{
    // Start is called before the first frame updatee
    void Start()
    {
       
    }
    private void Awake()
    {
        hp = 1;
    }
    // Update is called once per frame

}
